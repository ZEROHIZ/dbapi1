按照你的要求，我们先不急着改代码，而是纯粹从逻辑推演的角度，来拆解一下“点击打开浏览器”这个动作在服务端通常会涉及哪些环节，以及可能导致内存在“静止状态”下持续上涨的嫌疑点。
“打开浏览器”在服务端的典型逻辑链路
当你点击“打开浏览器”时，服务端（无论是基于 Puppeteer, Playwright, Selenium 还是自己封装的 CDP/WebDriver）通常会执行以下一系列隐式操作：
接收指令并初始化配置：分配端口、设置代理、生成用户数据目录（User Data Dir）等。
启动子进程 (Spawn Process)：在操作系统层面启动 chrome.exe 或其他浏览器进程。
建立通信通道：服务端通过 WebSocket（例如 CDP 协议）或 HTTP 与刚刚启动的浏览器进程建立长连接，以便后续发送控制指令。
绑定事件监听器 (Event Listeners)：服务端开始监听浏览器的各种状态（比如：页面崩溃、网络请求、控制台日志等）。
启动守护/轮询任务 (Background Tasks)：服务端可能会启动一些定时器（Timer）来监控浏览器的心跳、维持 Session 活跃、或者定期清理某些临时文件。
嫌疑点分析（为什么什么都不动，内存还会涨？）
既然“什么都不动，内存慢慢涨”，这意味着服务端一定有一个**“持续不断在执行，且不断产生新对象而无法被垃圾回收（GC）”**的逻辑。你需要重点去代码里排查以下几个嫌疑方向：
嫌疑点 1：未被消费的数据流（Stream 阻塞/堆积）
逻辑现象：服务端启动浏览器子进程后，通常会接管浏览器的标准输出（stdout）和标准错误（stderr）。即使你什么都不干，浏览器底层也会不断产生一些调试日志或内部事件。
内存上涨原因：如果服务端只是把这些流“pipe”过来了，但是没有将其输出到文件，也没有将数据丢弃（消费掉），这些日志数据就会一直积压在 Node.js/Python/Java 服务端进程的内存缓冲区（Buffer）中，导致内存缓慢但持续地上涨。
嫌疑点 2：无限累加的后台事件记录（Event Accumulation）
逻辑现象：建立 WebSocket (CDP) 连接后，浏览器会不断向服务端发送底层事件（即使你没动页面，也会有比如性能指标检测 Metrics、内部网络探测等事件）。
内存上涨原因：服务端可能注册了全局的监听器（比如 browser.on('targetcreated', ...) 或监听网络请求）。如果在这些回调函数中，代码将接收到的信息不断 push 到一个全局数组或 Map 中用于“记录日志”或“状态追踪”，且没有设置上限或定期清空机制，内存就会随着事件的积累不断变大。
嫌疑点 3：失控的定时器或轮询任务（Timers & Polling）
逻辑现象：“打开浏览器”的逻辑中，可能包含了一个 setInterval 或死循环线程，用于检查浏览器是否存活（心跳检测），或者用于监控某个特定的状态。
内存上涨原因：每次定时任务执行时，可能都在创建新的上下文对象、新建闭包、或者发起内部 HTTP 请求但没有正确关闭连接（连接泄露）。比如每秒执行一次状态查询，每次查询的结果都被永久保留在了内存里。
嫌疑点 4：不断重连的 WebSocket/内部通讯
逻辑现象：如果在打开浏览器后，服务端与浏览器的通讯出现微小的不稳定，服务端的库可能会尝试不断重连。
内存上涨原因：某些重连逻辑写得不好时，旧的连接对象（Connection/Socket）没有被销毁，新的连接对象又被创建，导致底层 Socket 对象在内存中无限增多。
嫌疑点 5：关闭浏览器时的资源未清理（针对“关闭后依然在涨”的现象）
你提到“测试过关闭浏览器后也还是一直在涨”。这是一个极其重要的线索！
这意味着：关闭浏览器只是杀掉了 chrome.exe 进程，但服务端为这个浏览器实例创建的那些“定时器”、“事件监听器”、“轮询任务”并没有被注销（Clear/Remove）。那个幽灵任务还在服务端后台空转，继续疯狂占用内存。
下一步的调查建议（不改代码，只做观察）
顺着上面的逻辑，你可以去代码或运行环境中做以下观察，找出“真凶”：
查阅“打开浏览器”函数的核心代码块：
找找看有没有 setInterval、setTimeout、while(true) 等轮询逻辑。
找找看有没有对数组、列表（List, Array, Map）只进行 add/push 而没有 remove/clear 的操作。
找找看有没有用到 stdout.on('data') 这样的流监听。
观察日志：
在服务端的控制台开启最详细的 Debug 级别日志。观察在“什么都不动”的静止期，控制台是否在疯狂打印某些心跳、内部状态检查的日志。
抓取内存快照（Heap Dump）：
如果服务端是 Node.js、Java 或 Python，可以在内存涨起来前后各抓取一次“堆内存快照”。
对比两次快照，看看多出来的究竟是什么对象（是 String 字符串？还是 Array 数组？还是某个特定的 Class 实例？）。一旦看到具体的对象名，立刻就能定位到是哪段逻辑在捣鬼。
总结你的排查结论：
目前高度怀疑是在“创建浏览器实例”的逻辑链条中，附带启动了一个用于监控、日志记录或通信的后台持续运行任务。这个任务不断产生数据堆积，且在“关闭浏览器”的逻辑中，遗漏了对此后台任务的销毁指令。