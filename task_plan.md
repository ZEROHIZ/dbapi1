# 任务计划

## 任务
为项目新增“浏览器登录式账号管理”能力，支持：
- 在后台页面管理多个浏览器档案账号
- 手动打开对应浏览器进行登录
- 展示浏览器指纹/设备信息
- 12 小时周期测活
- 为后续把真实浏览器态同步回请求链路打基础

## 阶段

### 阶段 1：方案与数据模型设计
状态：completed
目标：
- 明确“sessionid 注入方案”与“浏览器原生登录方案”的差异
- 设计浏览器账号档案的数据结构和后台操作流

输出：
- `browser_login_management_plan.md`
- `findings.md`

### 阶段 2：后端基础设施
状态：completed
目标：
- 新增浏览器档案模型与持久化字段
- 新增浏览器账号管理 API
- 新增打开浏览器、读取指纹、测活任务接口

关键点：
- 账号与浏览器档案一一对应
- 支持 Windows 本机路径和后续 Docker 分支
- 支持 12 小时测活计划

### 阶段 3：后台管理页面
状态：completed
目标：
- 在 `public/admin.html` 新增浏览器登录管理页
- 表格展示账号、浏览器档案、指纹、状态、最近测活时间
- 提供“打开浏览器登录”“刷新指纹”“立即测活”等操作

### 阶段 4：浏览器态接入请求链路
状态：pending
目标：
- 把浏览器真实登录态提取为请求可用状态
- 替换当前仅依赖 `sessionid + 随机 deviceId/webId` 的调用方式
- 优先验证生图能力，再扩展到聊天/视频/音乐

### 阶段 5：联调与验收
状态：in_progress
目标：
- 验证手动登录后，同一档案可重复打开且维持状态
- 验证 12 小时测活是否能稳定维护可用性
- 验证后台操作流程对多账号是否可用

## 风险
- 浏览器登录态可能不仅依赖 Cookie，还依赖本地存储和档案目录
- 指纹信息可能与具体机器、具体浏览器档案绑定
- 当前后台是单文件 `admin.html`，页面复杂度继续增长时维护成本会上升
- Windows 宿主机与生产 Docker 运行方式可能最终分叉

## 决策
- 暂不继续投入“仅注入 sessionid 保活”方案
- 新方案以“浏览器手动登录 + 持久化档案 + 后台管理”作为主路径
- 测活周期按 12 小时设计，但保留可配置项

## 2026-05-09 执行更新
- 已完成浏览器档案账号的数据模型扩展，并保持对旧版 `accounts.json` 的兼容读取。
- 已新增 `src/lib/browser-profile-manager.ts`，支持打开持久化档案、同步本地状态和执行探活。
- 已新增后台接口：浏览器账号的列表、创建、编辑、打开、刷新状态、立即探活、查看指纹、删除。
- 已在 `public/admin.html` 增加“浏览器账号”页面、创建/编辑弹窗和指纹详情弹窗。
- 已在系统设置中加入默认浏览器路径与浏览器探活周期配置。
- 为了避免影响现有渠道调度，浏览器账号暂时不会进入请求调度池；阶段 4 再把真实浏览器态接入请求链。
